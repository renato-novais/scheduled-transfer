import { Routes } from '@angular/router';

import { TransferSchedulePage } from './features/transfers/pages/transfer-schedule-page/transfer-schedule-page';

export const routes: Routes = [
  { path: '', component: TransferSchedulePage },
];
